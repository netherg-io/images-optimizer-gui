{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the local Windows optimizer UI",
  "windows": ["main"],
  "permissions": [
    "core:event:default",
    "core:path:default",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "dialog:allow-open"
  ]
}
